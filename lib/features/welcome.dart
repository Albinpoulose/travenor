import 'package:flutter/material.dart';

class Screenlcome extends StatelessWidget {
  const Screenlcome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}